/*Primeiro arquivo em linguagem c.
Para iniciar iremos importar a biblioteca
stdio (std-standard(padrão) io-input output
(entrada e saída)). Esta biblioteca possui
os comandos para interação com o teclado e 
monitor*/
#include <stdio.h>

/*iniciar a estrutura do código para a
mensagem hello world, usando a função 
main(principal), pois o compilador C irá
procurar esta função para dar execução no 
nosso código.*/
int main(){
    printf("Hello World!\n");
    return 0; 
}